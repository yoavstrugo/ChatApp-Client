import 'package:flutter/material.dart';
import 'package:intl/intl.dart';

const primaryColor = Color(0xFFFFFFFF);
const secondaryColor = Color(0xFF567AF4);
const foregroundColor = Color(0xFFF5F5F5);

const primaryTextColor = Color(0xFFFFFFFF);
const secondaryTextColor = Color(0xFF000000);

final dateFormat = DateFormat("HH:mm");

var fontFamily = "Segoe UI";

const IconData appIcon = IconData(0xe800, fontFamily: 'My Icons');
